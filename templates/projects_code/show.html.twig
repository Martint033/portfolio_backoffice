{% extends 'base.html.twig' %}

{% block title %}ProjectsCode{% endblock %}

{% block body %}
    <h1>ProjectsCode</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ projects_code.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('projects_code_index') }}">back to list</a>

    <a href="{{ path('projects_code_edit', {'id': projects_code.id}) }}">edit</a>

    {{ include('projects_code/_delete_form.html.twig') }}
{% endblock %}
