{% extends 'base.html.twig' %}

{% block title %}Projects{% endblock %}

{% block body %}
    <h1>Projects</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ project.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ project.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ project.description }}</td>
            </tr>
            <tr>
                <th>Context</th>
                {% for element in project.context %}
                    <td>{{ element }}</td>
                {% endfor %}
            </tr>
            <tr>
                <th>Démos</th>
                {% for element in project.demo %}
                    <td>{{ element }}</td>
                {% endfor %}
            </tr>
            <tr>
                <th>Code</th>
                {% for element in project.code %}
                    <td>{{ element }}</td>
                {% endfor %}
            </tr>
            <tr>
                <th>Images</th>
                {% for element in project.images %}
                    <td>{{ element }}</td>
                {% endfor %}
            </tr>
        </tbody>
    </table>

    <a href="{{ path('projects_index') }}">back to list</a>

    <a href="{{ path('projects_edit', {'id': project.id}) }}">edit</a>

    {{ include('projects/_delete_form.html.twig') }}
{% endblock %}
