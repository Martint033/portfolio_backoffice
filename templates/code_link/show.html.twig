{% extends 'base.html.twig' %}

{% block title %}CodeLink{% endblock %}

{% block body %}
    <h1>CodeLink</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ code_link.id }}</td>
            </tr>
            <tr>
                <th>Link</th>
                <td>{{ code_link.link }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('code_link_index') }}">back to list</a>

    <a href="{{ path('code_link_edit', {'id': code_link.id}) }}">edit</a>

    {{ include('code_link/_delete_form.html.twig') }}
{% endblock %}
